"""Attack profile builder modules.

Decomposed from vf_attack_profile.py (Task 2.3).

Each builder is a pure function that takes a SiteProfile (and needed
params) and returns a value — no self, no hidden mutable state.
"""
